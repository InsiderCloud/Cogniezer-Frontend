import 'package:flutter/material.dart';

class ADHDTestScreen extends StatelessWidget {
  const ADHDTestScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            "ADHD Test"
        ),
      ),
    );
  }
}
